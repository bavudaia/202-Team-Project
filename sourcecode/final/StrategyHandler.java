/**
 * Write a description of class StrategyHandler here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public interface StrategyHandler  
{
        public void handle(Player p);
         public void setStrategyHandler(StrategyHandler sh);
}
